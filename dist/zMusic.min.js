(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define("zMusic", [], factory);
	else if(typeof exports === 'object')
		exports["zMusic"] = factory();
	else
		root["zMusic"] = factory();
})(this, function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;
/******/
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// identity function for calling harmony imports with the correct context
/******/ 	__webpack_require__.i = function(value) { return value; };
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 3);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


console.log('currency');
var currency = {
    // leftDistance: (el) => {
    //     let left = el.offsetLeft;
    //     let scrollLeft;
    //     while (el.offsetParent) {
    //         el = el.offsetParent;
    //         left += el.offsetLeft;
    //     }
    //     scrollLeft = document.body.scrollLeft + document.documentElement.scrollLeft;
    //     return left - scrollLeft;
    // },
    // timeFormat: (time) => {
    //     let tempMin = parseInt(time / 60);
    //     let tempSec = parseInt(time % 60);
    //     let curMin = tempMin < 10 ? ('0' + tempMin) : tempMin;
    //     let curSec = tempSec < 10 ? ('0' + tempSec) : tempSec;
    //     return curMin + ':' + curSec;
    // },
    // percentFormat: (percent) => {
    //     return (percent * 100).toFixed(2) + '%';
    // },
    ajax: function ajax(option) {
        //传入的option是一个对象包括url和回调函数
        if (option.beforeSend) {
            option.beforeSend();
        }
        var url = option.url;
        if (option.songId) {
            url += '?channel=' + option.songId;
        }
        console.log(url);
        return new Promise(function (resolve, reject) {
            var xhr = new XMLHttpRequest();
            xhr.onreadystatechange = function () {
                if (xhr.readyState == 4) {
                    if (xhr.readyState == 4 && (xhr.status >= '200' || xhr.status == '304')) {
                        var data = JSON.parse(xhr.responseText);
                        resolve(data); //当得到数据时候数据传入这个回调函数
                    } else {
                        reject(xhr.status);
                    }
                }
            };
            xhr.onerror = function () {
                reject('ajax error'); //出现错误调用这个回调函数
            };
            xhr.open('get', url, true);
            xhr.send();
        });

        // let xhr = new XMLHttpRequest();
        // xhr.onreadystatechange = () => {
        //     if (xhr.readyState == 4) {
        //         if (xhr.readyState == 4 && (xhr.status == '200' || xhr.status == '304')) {
        //             let data = JSON.parse(xhr.responseText)
        //             if (option.success) {
        //                 option.success(data);
        //             }
        //         } else {
        //             if (option.fail) {
        //                 option.fail(xhr.status);
        //             }
        //         }
        //     }

        // };
        // xhr.open('GET', option.url);
        // xhr.send();
    }
};
module.exports = currency;

/***/ }),
/* 1 */,
/* 2 */
/***/ (function(module, exports) {

// removed by extract-text-webpack-plugin

/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

"use strict";


var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

__webpack_require__(2);

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

console.log('Z-Music 1.0');

var currency = __webpack_require__(0);

var albumUrl = 'http://api.jirengu.com/fm/getChannels.php';
var songUrl = 'http://api.jirengu.com/fm/getSong.php';
var lyricUrl = 'http://api.jirengu.com/fm/getLyric.php';

var zMusic = function () {
    function zMusic() {
        var _this = this;

        _classCallCheck(this, zMusic);

        this.ct = document.getElementById('z-music');

        this.albumData = {};
        this.songData = {};
        this.music = new Audio();

        this.ct.innerHTML = '<p class="z-music-loading">LOADING</p>';
        currency.ajax({
            url: albumUrl,
            beforeSend: function beforeSend() {
                console.log('正在获取专辑数据');
            }
        }).then(function (data) {
            console.log('成功获得专辑数据');
            _this.albumData = data.channels;
            console.log(_this.albumData);
            return currency.ajax({
                url: songUrl,
                songId: _this.albumData[0].channel_id,
                beforeSend: function beforeSend() {
                    console.log('正在获取歌曲数据');
                }
            });
        }, function (err) {
            console.log('获取专辑好像出错了!状态码:' + err);
        }).then(function (data) {
            console.log('成功获得歌曲数据');
            _this.songData = data.song;
            _this.ct.innerHTML = _this.template();
            console.log(_this.songData[0].url);
            _this.play(_this.songData[0].url);
            _this.init();
        }, function (err) {
            console.log('获取歌曲好像出错了!状态码:' + err);
        });
    }

    _createClass(zMusic, [{
        key: 'template',
        value: function template() {
            var html = ' \n                <div id="player">\n                    <div class="music-photo"><img src="' + this.songData[0].picture + '" alt=""></div>\n                    <h3>' + this.songData[0].title + '</h3>\n                    <p>' + this.songData[0].artist + '</p>\n                    <div class="line">\n                        <div class="line-loading"></div>\n                        <div class="play-time">1:30/4:30</div>\n                    </div>\n                    <div class="btns">\n                        <div class="btn loop">\n                            <img src="./src/imgs/random.png" alt="">\n                        </div>\n                        <div class="btn play">\n                            <img src="./src/imgs/play.png" alt="play">\n                        </div>\n                        <div class="btn next">\n                            <img src="./src/imgs/next.png" alt="play">\n                        </div>\n                        <div class="collect"><img src="./src/imgs/collect-yes.png" alt=""></div>\n                    </div>\n                </div>\n                <div id="album-list">\n                    <div class="list-ct">\n                        <ul class="clearfix">';

            for (var key in this.albumData) {
                html += '<li>\n                        <h4>' + this.albumData[key].name + '</h4>\n                        <div>\n                            <img src="' + this.albumData[key].cover_small + '" alt="">\n                        </div>\n                        <span class="ds"></span>\n                    </li>';
            }
            html += '\n                        </ul>\n                    </div >\n                </div > ';
            return html;
        }
    }, {
        key: 'init',
        value: function init() {
            console.log('init');
            this.dom = {
                btn_play: this.ct.querySelectorAll('.btns>.play'),
                btn_next: this.ct.querySelectorAll('.btns>.next'),
                btn_loop: this.ct.querySelectorAll('.btns>.loop'),
                albums: this.ct.querySelectorAll('.list-ct>ul>li'),
                albumCt: this.ct.querySelector('.list-ct>ul')
            };
            this.layout(); // 通过js设置album-list的宽度
        }
    }, {
        key: 'bind',
        value: function bind() {
            var _this2 = this;

            console.log('bind');
            this.updateLine = function () {
                var percent = _this2.audio.buffered.length ? _this2.audio.buffered.end(_this2.audio.buffered.length - 1) / _this2.audio.duration : 0;
                _this2.dom.timeline_loaded.style.width = Util.percentFormat(percent);
            };

            this.music.addEventListener('durationchange', function (e) {//duration属性改变（媒体总播放时间）
                // this.dom.timetext_total.innerHTML = Util.timeFormat(this.audio.duration);
                // this.updateLine();
            });
            this.music.addEventListener('progress', function (e) {//正在下载
                // this.updateLine();
            });
            this.music.addEventListener('canplay', function (e) {//缓存达到可以播放时候触发
                // if(this.option.autoplay && !this.isMobile){
                //     this.play();
                // }
            });
            this.music.addEventListener('timeupdate', function (e) {//currentTime（已播放时间）不合理或意外方式更新
                // let percent = this.audio.currentTime / this.audio.duration;
                // this.dom.timeline_played.style.width = Util.percentFormat(percent);
                // this.dom.timetext_played.innerHTML = Util.timeFormat(this.audio.currentTime);
            });
            this.music.addEventListener('seeked', function (e) {
                // this.play();
            });
            this.music.addEventListener('ended', function (e) {
                // this.next();
            });

            //---
            this.dom.playbutton.addEventListener('click', this.toggle);
        }
    }, {
        key: 'play',
        value: function play(url) {
            this.music.src = url;
            this.music.play();
        }
    }, {
        key: 'layout',
        value: function layout() {
            function style(element, pseduoElement) {
                return element.currentStyle ? element.currentStyle : window.getComputedStyle(element, pseduoElement);
            };
            var albumTotal = this.albumData.length;
            var theCSS = style(this.dom.albums[1], null);
            var albumWidth = parseInt(theCSS.width);
            var albumMargin = parseInt(theCSS.marginLeft);
            console.log(theCSS);
            this.dom.albumCt.style.width = (albumTotal + 1) * (albumWidth + albumMargin) + 'px';
        }
    }]);

    return zMusic;
}();

module.exports = zMusic;

/***/ })
/******/ ]);
});